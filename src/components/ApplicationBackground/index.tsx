import React from 'react';
import {Container} from './styles';

const ApplicationBackground: React.FC = ({children}) => {
  return <Container>{children}</Container>;
};

export {ApplicationBackground};
